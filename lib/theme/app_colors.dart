import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(
    0xFF0D1A36,
  ); // Azul escuro profundo (background principal)
  static const secondary = Color(0xFF123067); // Azul escuro intermediário
  static const accent = Color(
    0xFF345B9E,
  ); // Azul intermediário (botão, detalhes)
  static const highlight = Color(0xFF5A91E2); // Azul claro de destaque
  static const card = Color(0xFF19243A); // Cartões/superfícies mais claras
  static const background = Color(0xFF10172A); // Fundo absoluto
  static const textPrimary = Colors.white; // Branco puro para textos principais
  static const textSecondary = Color(
    0xFFB6C5E1,
  ); // Azul claro para textos secundários
}
